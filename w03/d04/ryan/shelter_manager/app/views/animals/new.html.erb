<ul>
	<%= form_for :animal, url: animals_path do |f| %>
		<% if @animal.errors.any? %>
		<div id="error_explanation">
		<h2><%=pluralize(@animal.errors.count, 'error') %> prohibited
		this article from being saved:</h2>
		<ul>
		<% @animal.errors.full_messages.each do |msg | %>
		<li><%= msg %></li>
		<% end %>
		</ul>
		</div>
		<% end %>
		<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
		</p>
		<p>
		<%= f.label :species %>
		<%= f.text_field :species %>
		</p>
		<p>
		<%= f.label :breed %>
		<%= f.text_field :breed %>
		</p>
		<p>
		<%= f.label :arrival_time %>
		<%= f.date_field :arrival_time %>
		</p>
		<p>
		<%= f.label :departure_time %>
		<%= f.date_field :departure_time %>
		</p>
		<p>
		<%= f.label :sex %>
		<%= f.text_field :sex %>
		</p>
		<p>
		<%= f.label :age %>
		<%= f.number_field :age %>
		</p>
		<p>
		<%= f.submit %>
		</p>
	<% end %>
</ul>

<%= link_to 'Back', animals_path %>